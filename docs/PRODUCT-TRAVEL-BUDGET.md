# 旅费有数

国内旅行预算规划器：手动填写 + 城市消费档位，第一版不接实时价格。网站和小红书小程序都支持生成可分享预算卡。

网站：`pnpm install && pnpm dev`

小红书小程序代码在 `xiaohongshu-mini/`，需要使用小红书官方开发者工具打开，并将 `project.config.json` 中的 AppID 替换为真实值。小红书小程序页面结构使用 `.xhsml`，需要单独提交审核。
